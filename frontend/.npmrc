# Synkord frontend npm/pnpm 配置
#
# 修复 pnpm + Electron 42 + Windows 下 require('electron') 返回字符串的 bug
# 把 electron 强制平铺到顶层 node_modules/electron，使 Electron 内部 C++ binding 能找到
shamefully-hoist=true
public-hoist-pattern[]=*
