# Synkord 产品设计文档

> 版本：v3.0 · 更新于 2026-06-10

---

## 一、产品定位

**Synkord** 是一个跨项目文件版本管理与变更通知工具。

> 核心思想：任何需要在多个项目/团队之间共享且需要追踪变更的文本文件，都可以成为"契约"。

### 典型契约文件类型

| 类型 | 示例 |
|------|------|
| 接口文档 | API README.md、OpenAPI YAML |
| 类型定义 | TypeScript interfaces、Go structs |
| 数据协议 | Protobuf .proto、Avro schema |
| 配置约定 | 环境变量说明、枚举值文档 |
| 数据库约定 | SQL 建表语句、字段说明 |
| 任意文本 | 自由格式说明文档 |

---

## 二、核心功能

```
定义契约  ──→  管理版本  ──→  通知变更  ──→  AI 感知
  ↑
可从本地文件导入
```

### 2.1 定义（Define）

- 创建"契约包"（Pack），内容为**任意文本文件**
- 支持从本地文件系统导入（按文件扩展名自动识别类型）
- 版本号使用语义化版本（SemVer）：`major.minor.patch`
- 内容类型（`contentType`）：`markdown` / `yaml` / `json` / `typescript` / `go` / `sql` / `proto` / `text`

### 2.2 分发（Distribute）

- **MCP Server**（嵌入 Electron，监听 `localhost:3742`）：AI 工具（Cursor、Copilot 等）通过 MCP 协议查询契约包原始内容，注入上下文
- **悬浮看板**：始终置顶浮动窗口，实时展示当前项目消费的契约包内容，支持关键字搜索

### 2.3 通知（Notify）

- 版本发布后，WebSocket 实时推送给订阅该契约包的组织成员
- 通知中心展示未读/历史通知，支持标记已读

---

## 三、本地项目集成

### 工作流程

```
1. 在 Synkord 关联本地项目目录
2. 浏览文件树，选择目标文件
3. 选择绑定到哪个契约包（新建或更新版本）
4. 一键发布为新版本 → 自动通知订阅者
```

### 数据存储

- 本地项目映射存储在 `localStorage`（纯本地，不上传）
- 绑定记录（`projectId → packName → filePath`）同样本地存储
- 契约包实际内容存储在后端 Git 裸仓库

### synkord.json（可选配置文件）

悬浮看板通过监听工作目录下的 `synkord.json` 自动感知当前项目消费的契约：

```json
{
  "project": "frontend-app",
  "org": "my-org",
  "consumes": ["user-api@1.x", "order-schema@*"]
}
```

---

## 四、技术架构

### 客户端（Electron）

```
┌─────────────────────────────────────┐
│  Electron Desktop App               │
│                                     │
│  ┌─────────────────┐  ┌──────────┐  │
│  │   主窗口         │  │ 悬浮看板  │  │
│  │  React + Router │  │ alwaysOnTop│  │
│  │  契约浏览/编辑   │  │ 内容预览  │  │
│  │  本地项目管理    │  │ 关键字搜索│  │
│  └─────────────────┘  └──────────┘  │
│                                     │
│  ┌─────────────────────────────┐    │
│  │   MCP Server (port 3742)    │    │
│  │   JSON-RPC over HTTP        │    │
│  │   tools: list_packs         │    │
│  │          get_pack           │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

### 后端（Go + Gin）

```
API Server (port 8080)
├── /api/auth/         # 登录/注册/Profile
├── /api/orgs/         # 组织管理
├── /api/orgs/:id/packs/         # 契约包 CRUD
├── /api/orgs/:id/packs/:p/versions  # 版本历史
├── /api/orgs/:id/packs/:p/diff     # 行级 diff
├── /api/notify/ws     # WebSocket 实时通知
└── /api/notify/       # 通知列表/已读
```

**契约存储**：每个组织一个 Git 裸仓库，契约包目录下存放 `contract` 文件（任意文本），版本通过 Git Tag（`packName/v1.0.0`）管理。

### 数据库（PostgreSQL）

| 表 | 用途 |
|----|------|
| `users` | 用户账号 |
| `orgs` | 组织 |
| `org_members` | 组织成员 |
| `contract_packs` | 契约包元数据（含 `content_type`） |
| `subscriptions` | 订阅关系 |
| `notifications` | 通知记录 |

---

## 五、diff 格式（行级）

`GET /api/orgs/:orgId/packs/:pack/diff?from=1.0.0&to=1.1.0`

```json
{
  "from": "1.0.0",
  "to": "1.1.0",
  "stats": { "added": 3, "removed": 1 },
  "hunks": [
    {
      "oldStart": 10,
      "newStart": 10,
      "lines": [
        { "type": "context", "oldNum": 10, "newNum": 10, "content": "  email: string" },
        { "type": "removed", "oldNum": 11, "content": "  age: number" },
        { "type": "added",   "newNum": 11, "content": "  age: number // 必须 > 0" },
        { "type": "added",   "newNum": 12, "content": "  phone: string" }
      ]
    }
  ]
}
```

---

## 六、MCP 工具列表

AI 通过 MCP Server 可调用：

| 工具 | 说明 |
|------|------|
| `list_packs` | 列举组织下所有契约包 |
| `get_pack` | 获取契约包原始内容（任意文本） |

AI 通过注入原始文本内容感知最新契约状态，实现"消除信息不对称"的目标。硬约束（如类型检查）由 CI 脚本负责。

---

## 七、认证

| 方式 | 状态 |
|------|------|
| 邮箱 + 密码 | ✅ 已实现 |
| 飞书 OAuth | ⏳ 待做 |

---

## 八、开发阶段完成情况

| ID | 阶段 | 状态 |
|----|------|------|
| X1 | 项目骨架 | ✅ |
| B1 | 用户认证 | ✅ |
| B2 | 组织管理 | ✅ |
| B3 | 飞书 OAuth | ⏳ 待做 |
| B4 | Git 仓库存储 | ✅ |
| B5 | 契约包 CRUD（任意文本） | ✅ |
| B6 | 行级文本 diff | ✅ |
| B7 | 订阅管理 | ✅ |
| B8 | WebSocket 通知 | ✅ |
| B9 | 通知 API | ✅ |
| F1 | 前端登录/注册 | ✅ |
| F2 | 组织创建/加入 | ✅ |
| F3 | 飞书登录页 | ⏳ 待做 |
| F4 | 契约包浏览（文本查看器） | ✅ |
| F5 | 行级 diff 视图 | ✅ |
| F6 | 契约包编辑器 + 文件导入 | ✅ |
| F7 | 悬浮看板 | ✅ |
| F8 | MCP Server | ✅ |
| F9 | 通知中心 | ✅ |
| F10 | 本地项目管理 | ✅ |
